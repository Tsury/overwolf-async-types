{
  "version": "0.0.1",
  "name": "overwolf-async-types",
  "author": "Amit Tsur",
  "repository": "github:tsury/overwolfAsycTypes",
  "type": "module",
  "types": "./overwolf.d.ts",
  "module": "./promisify.js",
  "scripts": {
    "build": "git submodule update --init && git submodule foreach \"git reset --hard head\" && node generateAsyncDefinitions.js",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "ts-morph": "^19.0.0"
  }
}
